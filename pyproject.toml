[project]
name = "security-vendor-selection-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.1",
    "flask-babel>=4.0.0",
    "gunicorn>=23.0.0",
]
